---
import { twMerge } from 'tailwind-merge';

interface Props {
  variant?: string;
  xClass?: string;
}

// TODO: Use or delete "variant"
const { variant, xClass } = Astro.props;
---


<button class={twMerge('bg-accent border-2 border-background text-background rounded-full px-4 py-2', xClass)}>
  <slot />
</button>